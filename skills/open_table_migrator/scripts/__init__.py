"""Python implementation for the open-table-migrator skill.

Per the Qwen Code skills layout, the executable Python lives under
``scripts/``. Internal modules import each other via relative paths
(``from .detector import ...``) — they remain siblings inside this
package.
"""
